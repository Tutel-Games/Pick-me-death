using UnityEngine;

public class InputReader : MonoBehaviour
{
    public bool IsJumping;
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {
            
        }
    }
}
